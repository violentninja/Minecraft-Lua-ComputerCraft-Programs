--Preparing the message sending
rednet.open("right") --Change side to the side of your modem if required
--Sending the message
rednet.send(1, "Hello World!") --Change the reciever id to the reciever computer's ID
